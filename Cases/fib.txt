program test;
    var fib_series, i, n, first, second, next:int;

    main {
        n = 10;

        first = 0;
        second = 1;
        i = 2;
        print(first, " ");
        print(second, " ");

        do {
            next = first + second;
            print(next, " ");
            first = second;
            second = next;
            i = i + 1;
        } while(i < n);
    }
end